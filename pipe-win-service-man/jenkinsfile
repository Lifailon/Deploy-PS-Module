pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        sh 'pwsh --version'
      }
    }
    stage('get-process') {
      steps {
        sh 'pwsh -command write-host Start'
		sh 'pwsh pipe-win-service-man/get-process.ps1'
      }
    }
  }
}