pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        sh 'pwsh --version'
      }
    }
    stage('get-process') {
      steps {
        sh 'pwsh -command write-host Start'
		sh 'pwsh get-process.ps1'
      }
    }
  }
}