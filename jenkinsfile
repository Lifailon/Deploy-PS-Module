pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        cmd 'pwsh --version'
      }
    }
    stage('hello') {
      steps {
        cmd 'pwsh -command get-service'
      }
    }
  }
}